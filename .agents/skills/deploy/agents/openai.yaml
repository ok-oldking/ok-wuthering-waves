interface:
  display_name: "Deploy"
  short_description: "Commit, tag, and push release versions safely."
  default_prompt: "Use $deploy to commit the current changes, tag a release version, and push it."
